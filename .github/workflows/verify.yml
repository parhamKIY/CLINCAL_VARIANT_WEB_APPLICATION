name: Verify

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  stage-44:
    runs-on: windows-latest
    timeout-minutes: 20
    env:
      LLM_PROVIDER: openai_compatible
      LLM_BASE_URL: https://provider.example/v1
      LLM_API_KEY: offline-test-key
      LLM_MODEL: offline-test-model
      LLM_MODEL_LIGHT: offline-light-model
      LLM_MODEL_STRONG: offline-strong-model

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: "3.13"
          cache: pip
          cache-dependency-path: |
            requirements.txt
            requirements-dev.txt

      - name: Install dependencies
        run: python -m pip install -r requirements-dev.txt

      - name: Run final acceptance gate
        run: python tests/run_stage44_acceptance.py
